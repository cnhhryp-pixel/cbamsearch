'use client';import{useEffect,useMemo,useState}from'react';import{useSearchParams}from'next/navigation';import{cbamCodes,searchCbam}from'../../data/cbam-codes';
export default function SearchClient(){const sp=useSearchParams();const[q,setQ]=useState('');useEffect(()=>{const incoming=sp.get('q');if(incoming)setQ(incoming)},[sp]);const results=useMemo(()=>q?searchCbam(q):cbamCodes,[q]);return <><div className="search"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search 7604, aluminium profiles, cement..." autoFocus/><button type="button">Search</button></div><p className="small">{q?results.length+' matching records':'Showing '+results.length+' MVP records'} · Scope data should be verified against current EU legislation.</p><div className="results">{results.map(x=><a className="result" href={'/cn-code/'+x.code+'/'} key={x.code}><div><b>{x.display}</b><h3>{x.name}</h3></div><div className="resultMeta"><span className="chip">{x.sector}</span><span>{x.gas}</span><span>{x.level}</span></div></a>)}{q&&!results.length&&<div className="emptyState card"><b>No verified match in the current dataset.</b><p>This is not a “not covered” decision. Try a shorter CN/HS heading or a broader product term.</p><div className="chips"><a className="chip" href="/cbam-checker/">Open Scope Checker</a><a className="chip" href="/products/">Browse sectors</a><a className="chip" href="https://eur-lex.europa.eu/eli/reg/2023/956/oj">Verify Annex I</a></div></div>}</div></>}