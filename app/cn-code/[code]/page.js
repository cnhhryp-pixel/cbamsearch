import {cbamCodes,getScopeMatch,CBAM_SCOPE_SOURCE} from '../../../data/cbam-codes';import {notFound} from 'next/navigation';import Breadcrumbs from '../../components/Breadcrumbs';import FAQ from '../../components/FAQ';
export function generateStaticParams(){return cbamCodes.map(x=>({code:x.code}))}
export function generateMetadata({params}){const x=cbamCodes.find(i=>i.code===params.code);return x?{title:`CN Code ${x.display} CBAM — ${x.name} Scope & Calculator`,description:`Research CBAM scope for CN code ${x.display} (${x.name}), review classification context and continue to scope checking or calculation.`,alternates:{canonical:`/cn-code/${x.code}/`},robots:{index:x.level==='CN'||x.level==='Subheading',follow:true}}:{}}
export default function Page({params}){const x=cbamCodes.find(i=>i.code===params.code);if(!x)notFound();const exclusions=(x.excludedCodes||[]).map(code=>code.replace(/\s/g,''));const isBroad=['Chapter','Heading','Subheading'].includes(x.level);const scope=getScopeMatch(x.code);const scopeLabel=scope.status==='covered'?'Covered record':scope.status==='potential'?'Potential scope':scope.status==='excluded'?'Excluded':'Classification check';const related=cbamCodes.filter(i=>i.code!==x.code&&i.sector===x.sector).sort((a,b)=>{const ap=a.code.startsWith(x.code)||x.code.startsWith(a.code)?0:1,bp=b.code.startsWith(x.code)||x.code.startsWith(b.code)?0:1;return ap-bp||a.code.localeCompare(b.code)}).slice(0,6);const faqs=[{question:`Is CN code ${x.display} covered by CBAM?`,answer:isBroad?`${x.display} is represented as a broader classification in the CBAMSearch scope dataset. Check the exact CN code because narrower inclusions or exclusions may apply.`:`${x.display} is recorded in the CBAMSearch scope dataset under the ${x.sector} sector. Confirm the exact customs classification before relying on the result.`},{question:'Can I calculate CBAM cost from the CN code alone?',answer:'No. A cost estimate also needs applicable embedded-emissions inputs, the relevant certificate-price period and other applicable adjustments.'}];return <main><section className="detailHero codeHero"><div className="wrap"><Breadcrumbs items={[{name:'CN Code',href:'/cn-code/'},{name:x.display,href:`/cn-code/${x.code}/`}]}/><div className="eyebrow">{x.sector.toUpperCase()} · {x.level.toUpperCase()}</div><h1>CN {x.display}: {x.name}</h1><p className="lead">Review the recorded CBAM scope context for this classification and continue into an exact scope check, calculation or saved assessment.</p><div className="detailActions"><a className="btn" href={`/cbam-checker/?q=${x.code}`}>Check exact scope</a><a className="btn secondaryBtn" href="/cbam-calculator/">Calculate exposure</a></div></div></section><section className="section"><div className="wrap"><div className="detailStats"><div><span>Scope status</span><strong>{scopeLabel}</strong></div><div><span>Sector</span><strong>{x.sector}</strong></div><div><span>Gas category</span><strong>{x.gas}</strong></div></div>{exclusions.length>0&&<div className="warningBox"><b>Recorded exclusions under this parent classification</b><p>{exclusions.join(' · ')}</p><span>Use the exact CN code before relying on a broader heading.</span></div>}<div className="detailSection"><div className="sectionLabel">HOW TO USE THIS RECORD</div><h2>From CN code to CBAM assessment</h2><div className="countryWorkflow threeSteps"><div><span>01</span><h3>Confirm classification</h3><p>Use the most specific classification applicable to the imported goods.</p></div><div><span>02</span><h3>Establish emissions input</h3><p>Determine the applicable embedded-emissions data path and source.</p></div><div><span>03</span><h3>Calculate & document</h3><p>Estimate exposure, then save important scenarios into an assessment.</p></div></div></div>{related.length>0&&<section className="detailSection"><div className="sectionLabel">RELATED CLASSIFICATIONS</div><h2>More {x.sector} CN codes</h2><div className="results">{related.map(i=><a className="result" href={`/cn-code/${i.code}/`} key={i.code}><div><b>{i.display}</b><h3>{i.name}</h3></div><span className="chip">{i.level}</span></a>)}</div></section>}<div className="detailConversion"><div><div className="eyebrow lightEyebrow">SAVE THE SCENARIO</div><h2>Turn CN {x.display} into a structured assessment.</h2><p>Add origin, quantity and emissions-data availability in the CBAM workspace.</p></div><a className="btn lightBtn" href="/cbam-report/">Start free assessment →</a></div><FAQ items={faqs}/><div className="sourceStrip"><div><b>Source & methodology</b><p>{CBAM_SCOPE_SOURCE.title} · Checked: {CBAM_SCOPE_SOURCE.checked}. Method: exact code → parent scope → recorded exclusion check.</p></div><a href="https://eur-lex.europa.eu/eli/reg/2023/956/oj">Primary legislation →</a></div></div></section></main>}