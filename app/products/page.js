import{sectors}from'../../data/sectors';export const metadata={title:'CBAM Sectors & Products',description:'Browse EU CBAM sectors and searchable product code references.',alternates:{canonical:'/products/'}};export default function Page(){return <main className="section"><div className="wrap"><div className="eyebrow">Covered sectors</div><h1>CBAM products and sectors</h1><p className="lead">Browse the six core sector groups and continue into source-linked CN code records.</p><div className="grid">{sectors.map(x=><a className="card" href={'/products/'+x.slug+'/'} key={x.slug}><b>{x.name} →</b><p>{x.summary}</p><span className="chip">{x.gas}</span></a>)}</div></div></main>}