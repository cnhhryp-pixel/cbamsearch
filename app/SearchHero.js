'use client';import{useState}from'react';import{useRouter}from'next/navigation';export default function SearchHero(){const[q,setQ]=useState('');const r=useRouter();function go(e){e.preventDefault();if(q.trim())r.push('/cn-code/?q='+encodeURIComponent(q.trim()))}return <form className="search" onSubmit={go}><input value={q} onChange={e=>setQ(e.target.value)} aria-label="CN code, HS code or product" placeholder="Enter CN code, HS code or product — e.g. 7604 or aluminium profiles"/><button>Search CBAM</button></form>}