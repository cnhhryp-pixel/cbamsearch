"use client";
import {useEffect,useState} from 'react';
import {supabaseConfig} from '../../lib/supabase';

export default function Page(){const [status,setStatus]=useState('Checking account session...');const [projects,setProjects]=useState([]);
useEffect(()=>{async function load(){if(!supabaseConfig.url||!supabaseConfig.anonKey){setStatus('Supabase connection required.');return;}setStatus('Workspace ready.');}load();},[]);
return <main className="section"><div className="wrap"><div className="eyebrow">CBAM WORKSPACE</div><h1>CBAM Dashboard</h1><p className="lead">Manage your CBAM assessments, reports and compliance projects in one workspace.</p><div className="notice"><p>{status}</p></div><div className="grid"><div className="card"><h2>Projects</h2><p>Manage product assessments and CBAM review projects.</p><button>+ New Assessment</button></div><div className="card"><h2>Reports</h2><p>View generated CBAM assessment reports and export history.</p></div><div className="card"><h2>Account Plan</h2><p>Current plan: Free</p><p>Upgrade options will be available after account activation.</p></div></div><section className="section"><div className="notice"><h2>My Assessments</h2>{projects.length===0?<p>No assessments created yet.</p>:projects.map((p,i)=><div key={i}><b>{p.product_name}</b><p>{p.cn_code} · {p.country_origin}</p><small>{p.status}</small></div>)}<p>Create your first CBAM assessment to start managing compliance information.</p></div></section></div></main>}